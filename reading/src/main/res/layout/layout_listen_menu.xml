<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto"
             android:layout_width="match_parent"
             android:layout_height="wrap_content"
             android:orientation="vertical">

    <ImageView
        android:id="@+id/image_listen_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:scaleType="fitXY"
        android:src="@mipmap/bg_listen_menu"/>


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dp_10"
        app:layout_constraintBottom_toBottomOf="parent">

        <ImageView
            android:visibility="gone"
            android:id="@+id/image_listen_before"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@mipmap/ic_alwayslisten_left"
            android:layout_alignBottom="@+id/image_listen_playAndStop"
            android:layout_marginLeft="60dp"
            android:layout_marginBottom="@dimen/dp_10"/>

        <android.support.design.widget.CheckableImageButton
            android:id="@+id/image_listen_playAndStop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:src="@drawable/ic_listen_toggle"
            android:background="@null"/>

        <ImageView
            android:visibility="gone"
            android:id="@+id/image_listen_after"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@mipmap/ic_alwayslisten_right"
            android:layout_alignBottom="@+id/image_listen_playAndStop"
            android:layout_alignParentRight="true"
            android:layout_marginBottom="@dimen/dp_10"
            android:layout_marginRight="60dp"/>

    </RelativeLayout>

</FrameLayout>
